{
  "name": "vorce-electron",
  "version": "1.0.0",
  "description": "VORCE desktop applications built with Electron.",
  "private": true,
  "scripts": {
    "start": "cd electron-agent && npm start",
    "dev": "cd electron-agent && npm run dev",
    "install:agent": "cd electron-agent && npm install",
    "rebuild": "cd electron-agent && npm run rebuild"
  },
  "author": "VORCE",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=18"
  }
}
