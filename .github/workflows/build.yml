name: Build Vlinked

on:
  push:
    branches:
      - master
    tags:
      - 'v*'
      - '[0-9]*'
  workflow_dispatch:

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win
          - os: macos-latest
            platform: mac

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Python (macOS native build dependency)
        if: matrix.platform == 'mac'
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        working-directory: electron-agent
        run: npm install

      # ═══════════════════════════════════════════════
      # CRITICAL FIX 1: Rebuild native addon against Electron ABI
      # node-gyp alone compiles against Node.js headers.
      # electron-rebuild recompiles against the exact Electron runtime.
      # ═══════════════════════════════════════════════
      - name: Rebuild native modules for Electron (macOS)
        if: matrix.platform == 'mac'
        working-directory: electron-agent
        run: npx electron-rebuild -f -w wifi-native

      # ═══════════════════════════════════════════════
      # CRITICAL FIX 2: Verify native binary exists after rebuild
      # Build MUST fail if wifi_native.node is missing.
      # ═══════════════════════════════════════════════
      - name: Verify native binary exists
        if: matrix.platform == 'mac'
        working-directory: electron-agent
        run: |
          echo "=== Searching for wifi_native.node ==="
          FOUND=$(find . -name "wifi_native.node" -type f 2>/dev/null)
          if [ -z "$FOUND" ]; then
            echo "::error::CRITICAL: wifi_native.node not found after electron-rebuild. Native addon compilation failed."
            exit 1
          fi
          echo "✅ Found native binary:"
          echo "$FOUND"
          for f in $FOUND; do
            echo "--- file info: $f ---"
            file "$f"
          done

      - name: Decode certificate (macOS only)
        if: matrix.platform == 'mac'
        working-directory: electron-agent
        run: |
          echo "${{ secrets.APPLE_CERTIFICATE_BASE64 }}" | base64 --decode > certificate.p12

      - name: Build Electron app (macOS)
        if: matrix.platform == 'mac'
        working-directory: electron-agent
        run: npm run build:mac
        env:
          CSC_LINK: certificate.p12
          CSC_KEY_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          APPLE_TEAM_ID: TK5W5C4S89

      - name: Build Electron app (Windows/Linux)
        if: matrix.platform != 'mac'
        working-directory: electron-agent
        run: npm run build:${{ matrix.platform }}

      # ═══════════════════════════════════════════════
      # CRITICAL FIX 3: Verify binary is packaged in app.asar.unpacked
      # After electron-builder, the .node MUST be outside the ASAR archive.
      # ═══════════════════════════════════════════════
      - name: Verify native binary in app.asar.unpacked
        if: matrix.platform == 'mac'
        working-directory: electron-agent
        run: |
          echo "=== Searching for .node files in dist ==="
          ALL_NODES=$(find dist -name "*.node" -type f 2>/dev/null)
          echo "All .node files found:"
          echo "$ALL_NODES"
          echo ""

          echo "=== Verifying app.asar.unpacked location ==="
          UNPACKED=$(find dist -path "*app.asar.unpacked*" -name "wifi_native.node" -type f 2>/dev/null)
          if [ -z "$UNPACKED" ]; then
            echo "::error::CRITICAL: wifi_native.node NOT found in app.asar.unpacked. The native addon will fail to load at runtime."
            exit 1
          fi
          echo "✅ Native binary correctly unpacked:"
          echo "$UNPACKED"

      # ═══════════════════════════════════════════════
      # CRITICAL FIX 7: Verify entitlements after codesign
      # Location entitlement MUST be present for CoreWLAN SSID access.
      # ═══════════════════════════════════════════════
      - name: Verify entitlements
        if: matrix.platform == 'mac'
        working-directory: electron-agent
        run: |
          echo "=== Locating .app bundle ==="
          APP_PATH=$(find dist -name "*.app" -type d | head -1)
          if [ -z "$APP_PATH" ]; then
            echo "::warning::No .app bundle found in dist (may be cleaned after packaging). Skipping entitlements check."
            exit 0
          fi
          echo "Found: $APP_PATH"
          echo ""

          echo "=== Extracting entitlements ==="
          ENTITLEMENTS=$(codesign -d --entitlements :- "$APP_PATH" 2>&1) || true
          echo "$ENTITLEMENTS"
          echo ""

          if echo "$ENTITLEMENTS" | grep -q "com.apple.security.personal-information.location"; then
            echo "✅ Location entitlement verified"
          else
            echo "::error::CRITICAL: com.apple.security.personal-information.location entitlement NOT found in signed app"
            exit 1
          fi

      # ═══════════════════════════════════════════════
      # CRITICAL FIX 8: Verify binary architecture
      # Must support the target architecture (arm64 for Apple Silicon).
      # ═══════════════════════════════════════════════
      - name: Verify binary architecture
        if: matrix.platform == 'mac'
        working-directory: electron-agent
        run: |
          NODE_FILE=$(find dist -path "*app.asar.unpacked*" -name "wifi_native.node" -type f | head -1)
          if [ -z "$NODE_FILE" ]; then
            echo "::error::Cannot verify architecture - binary not found in unpacked"
            exit 1
          fi
          echo "=== Architecture info ==="
          lipo -info "$NODE_FILE"
          echo ""
          echo "=== File type ==="
          file "$NODE_FILE"

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: vlinked-${{ matrix.platform }}
          path: |
            electron-agent/dist/*.exe
            electron-agent/dist/*.dmg
            electron-agent/dist/*.zip
            electron-agent/dist/*.AppImage
            electron-agent/dist/*.deb
            electron-agent/dist/latest.yml
            electron-agent/dist/latest-mac.yml
            electron-agent/dist/latest-linux.yml
          if-no-files-found: ignore


      - name: Authenticate to Google Cloud
        if: matrix.platform == 'mac' || matrix.platform == 'win'
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Cloud SDK
        if: matrix.platform == 'mac' || matrix.platform == 'win'
        uses: google-github-actions/setup-gcloud@v2

      - name: Delete old macOS DMG in Firebase Storage
        if: matrix.platform == 'mac'
        run: |
          gcloud storage rm 'gs://hora-7394b.firebasestorage.app/Vlinked_Mac/**' || true

      - name: Delete old Windows EXE in Firebase Storage
        if: matrix.platform == 'win'
        shell: bash
        run: |
          gcloud storage rm 'gs://hora-7394b.firebasestorage.app/Vlinked_Windows/**' || true

      - name: Upload macOS DMG to Firebase Storage
        if: matrix.platform == 'mac'
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: 'electron-agent/dist'
          glob: '*.dmg'
          destination: 'hora-7394b.firebasestorage.app/Vlinked_Mac'
          parent: false

      - name: Set Firebase Storage Token and Filename for macOS DMG
        if: matrix.platform == 'mac'
        shell: bash
        run: |
          gsutil setmeta -h "x-goog-meta-firebaseStorageDownloadTokens:d5dfca38-6797-4c94-8a15-e506a70ef363" -h "Content-Disposition: attachment; filename=\"Vlinked-1.0.0-arm64.dmg\"" "gs://hora-7394b.firebasestorage.app/Vlinked_Mac/*.dmg"

      - name: Upload Windows EXE to Firebase Storage
        if: matrix.platform == 'win'
        uses: google-github-actions/upload-cloud-storage@v2
        with:
          path: 'electron-agent/dist'
          glob: '*.exe'
          destination: 'hora-7394b.firebasestorage.app/Vlinked_Windows'
          parent: false

      - name: Set Firebase Storage Token and Filename for Windows EXE
        if: matrix.platform == 'win'
        shell: bash
        run: |
          gsutil setmeta -h "x-goog-meta-firebaseStorageDownloadTokens:1a0689a8-0f9a-46ac-b842-522167326ad0" -h "Content-Disposition: attachment; filename=\"Vlinked-Setup-1.0.0.exe\"" "gs://hora-7394b.firebasestorage.app/Vlinked_Windows/*.exe"

  release:
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/**/*.exe
            artifacts/**/*.dmg
            artifacts/**/*.zip
            artifacts/**/*.AppImage
            artifacts/**/*.deb
            artifacts/**/latest.yml
            artifacts/**/latest-mac.yml
            artifacts/**/latest-linux.yml
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
